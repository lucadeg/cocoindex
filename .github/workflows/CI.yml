
name: CI

on:
  workflow_dispatch:

  # Automatic triggers removed; invoke through FounderOS/Admin Gate.
permissions:
  contents: read

jobs:
  build-test:
    name: Build & Test
    uses: ./.github/workflows/_test.yml
